import { ButtonColors, ButtonVariants } from 'components/button';

export const filterConfig = {
  btnText: 'Фильтры',
  textColor: ButtonColors.RED,
  bgColor: ButtonColors.WHITE,
  variants: ButtonVariants.ELLIPSE,
};
export const priseSortConfig = {
  btnText: 'По цене',
  textColor: ButtonColors.DARK_GREY,
};
