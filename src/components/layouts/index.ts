import { AppLayout as App } from './app';
import { SidebarLayout as Sidebar } from './sidebar';
import { ProductInfoLayout as ProductInfo } from './product-info';

export const Layouts = {
  App,
  Sidebar,
  ProductInfo,
};
