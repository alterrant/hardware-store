import { CharacteristicsSection } from './characteristics-section';
import { MainInfoSection } from './main-info-section';
import { SimilarProducts } from './similar-products-section';

export const ProductSections = {
  Characteristics: CharacteristicsSection,
  MainInfo: MainInfoSection,
  SimilarProducts,
};
